# =============================================================================
#  TimeTracker — Nginx virtual host (HTTP only, no SSL)
#  Для production с SSL см. комментарии внизу файла
# =============================================================================

upstream timetracker_api {
    server api:8000;
    keepalive 32;
}

server {
    listen      80;
    listen [::]:80;
    server_name _;

    client_max_body_size 10m;

    # -- Health (без rate limit) ----------------------------------------------
    location = /health {
        proxy_pass         http://timetracker_api;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        access_log         off;
    }

    # -- WebSocket ------------------------------------------------------------
    location /ws/ {
        proxy_pass             http://timetracker_api;
        proxy_http_version     1.1;
        proxy_set_header       Upgrade    $http_upgrade;
        proxy_set_header       Connection "upgrade";
        proxy_set_header       Host       $host;
        proxy_set_header       X-Real-IP  $remote_addr;
        proxy_read_timeout     3600s;
        proxy_send_timeout     3600s;
    }

    # -- API ------------------------------------------------------------------
    location /api/ {
        proxy_pass         http://timetracker_api;
        proxy_http_version 1.1;
        proxy_set_header   Connection        "";
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_connect_timeout 10s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }

    # -- Admin panel ----------------------------------------------------------
    location /admin {
        proxy_pass         http://timetracker_api;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # -- Root -----------------------------------------------------------------
    location / {
        proxy_pass         http://timetracker_api;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}

# =============================================================================
#  Для HTTPS раскомментируй и настрой блок ниже (нужны сертификаты):
#
#  server {
#      listen 443 ssl http2;
#      ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#      ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#      ... (остальные настройки как выше)
#  }
# =============================================================================
