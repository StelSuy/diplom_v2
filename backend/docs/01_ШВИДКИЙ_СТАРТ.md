# 🚀 ШВИДКИЙ СТАРТ - TimeTracker API

**Дата оновлення:** 30 січня 2026  
**Версія:** 2.0

---

## 📋 ЗМІСТ

1. [Вимоги до системи](#-вимоги-до-системи)
2. [Локальний запуск (Windows)](#-локальний-запуск-windows)
3. [Перевірка роботи](#-перевірка-роботи)
4. [Типові проблеми](#-типові-проблеми)
5. [Корисні команди](#-корисні-команди)

---

## 💻 Вимоги до системи

### Обов'язкові компоненти:
- ✅ **Python 3.12+** - мова програмування
- ✅ **MySQL 8.0+** або **MariaDB 11+** - база даних
- ✅ **Git** - для клонування репозиторію

### Опціональні компоненти:
- 🟡 **MySQL Workbench** - для управління БД (рекомендується)
- 🟡 **Visual Studio Code** - редактор коду
- 🟡 **Postman** - для тестування API

---

## 🚀 Локальний запуск (Windows)

### Крок 1: Налаштування бази даних

#### 1.1 Перевірте статус MySQL

```powershell
# Перевірка чи працює MySQL
Get-Service MySQL*

# Якщо не запущений - запустіть
Start-Service MySQL80
```

#### 1.2 Створіть базу даних

```sql
-- Увійдіть в MySQL (через командний рядок або MySQL Workbench)
mysql -u root -p

-- Виконайте команди:
CREATE DATABASE IF NOT EXISTS diploma_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE USER IF NOT EXISTS 'timetracker'@'localhost' IDENTIFIED BY 'StrongPass123!';

GRANT ALL PRIVILEGES ON diploma_db.* TO 'timetracker'@'localhost';

FLUSH PRIVILEGES;

EXIT;
```

### Крок 2: Налаштування проекту

#### 2.1 Клонуйте репозиторій (якщо ще не зробили)

```powershell
cd F:\Вуз\ДИПЛОМ\SYSTEM
# Проект вже має бути тут
```

#### 2.2 Створіть віртуальне оточення

```powershell
cd F:\Вуз\ДИПЛОМ\SYSTEM\backend

# Створити venv (якщо ще немає)
python -m venv .venv

# Активувати
.\.venv\Scripts\Activate.ps1
```

#### 2.3 Встановіть залежності

```powershell
# Оновити pip
python -m pip install --upgrade pip

# Встановити всі залежності
pip install -r requirements.txt
```

#### 2.4 Налаштуйте .env файл

```powershell
# Якщо файлу .env немає - створіть його з прикладу
if (!(Test-Path .env)) {
    Copy-Item .env.example .env
}

# Відредагуйте .env файл (notepad або VSCode)
notepad .env
```

**Мінімальні налаштування в .env:**
```ini
# Застосунок
APP_NAME=TimeTracker API
ENV=development
DEBUG=true

# База даних
DATABASE_URL=mysql+pymysql://timetracker:StrongPass123!@127.0.0.1:3306/diploma_db

# Безпека (ВАЖЛИВО: змініть в production!)
JWT_SECRET=ваш-дуже-довгий-секретний-ключ-мінімум-32-символи
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# CORS (додайте ваші фронтенд адреси)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
```

### Крок 3: Міграції бази даних

```powershell
# Застосувати всі міграції
alembic upgrade head

# Перевірити поточну версію
alembic current
```

**Очікуваний результат:**
```
INFO  [alembic.runtime.migration] Context impl MySQLImpl.
INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
INFO  [alembic.runtime.migration] Running upgrade  -> 001, initial_schema
INFO  [alembic.runtime.migration] Running upgrade 001 -> 002, make_terminal_nullable
```

### Крок 4: Запуск сервера

```powershell
# Запустити development сервер
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# АБО використати готовий скрипт:
.\run_dev.bat
```

**Очікуваний вивід:**
```
INFO:     Will watch for changes in these directories: ['F:\\Вуз\\ДИПЛОМ\\SYSTEM\\backend']
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [12345] using WatchFiles
INFO:     Started server process [67890]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

---

## ✅ Перевірка роботи

### 1. Відкрийте в браузері:

#### 🏠 Головна сторінка
```
http://localhost:8000/
```
Має показати інформацію про API.

#### 📚 API документація (Swagger UI)
```
http://localhost:8000/docs
```
Інтерактивна документація з можливістю тестування.

#### 📖 Альтернативна документація (ReDoc)
```
http://localhost:8000/redoc
```
Красива документація для читання.

#### 💓 Health Check
```
http://localhost:8000/health
```
Має повернути:
```json
{
  "status": "ok",
  "app": "TimeTracker API",
  "env": "development",
  "version": "1.0.0"
}
```

### 2. Тестовий вхід (через Swagger UI)

1. Відкрийте http://localhost:8000/docs
2. Знайдіть endpoint `POST /api/auth/login`
3. Натисніть "Try it out"
4. Введіть дані:
```json
{
  "username": "admin",
  "password": "admin123"
}
```
5. Натисніть "Execute"

**Очікувана відповідь:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

### 3. Перевірте логи

Сервер має виводити логи про всі запити:
```
INFO:     127.0.0.1:54321 - "GET / HTTP/1.1" 200 OK
INFO:     127.0.0.1:54322 - "POST /api/auth/login HTTP/1.1" 200 OK
```

---

## 🔧 Типові проблеми

### ❌ Проблема: MySQL не запущений

**Симптоми:**
```
sqlalchemy.exc.OperationalError: (pymysql.err.OperationalError) (2003, "Can't connect to MySQL server...")
```

**Рішення:**
```powershell
# Перевірити статус
Get-Service MySQL*

# Запустити
Start-Service MySQL80

# Перевірити порт
netstat -ano | findstr :3306
```

---

### ❌ Проблема: База даних не існує

**Симптоми:**
```
sqlalchemy.exc.OperationalError: (pymysql.err.OperationalError) (1049, "Unknown database 'diploma_db'")
```

**Рішення:**
```sql
-- Створіть базу даних
mysql -u root -p
CREATE DATABASE diploma_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

---

### ❌ Проблема: Неправильний пароль БД

**Симптоми:**
```
sqlalchemy.exc.OperationalError: (pymysql.err.OperationalError) (1045, "Access denied for user 'timetracker'@'localhost'")
```

**Рішення:**
1. Перевірте пароль в .env файлі
2. Або скиньте пароль:
```sql
mysql -u root -p
ALTER USER 'timetracker'@'localhost' IDENTIFIED BY 'StrongPass123!';
FLUSH PRIVILEGES;
EXIT;
```

---

### ❌ Проблема: Порт 8000 вже зайнятий

**Симптоми:**
```
ERROR:    [Errno 10048] error while attempting to bind on address ('0.0.0.0', 8000): only one usage of each socket address
```

**Рішення:**
```powershell
# Знайти процес на порту 8000
netstat -ano | findstr :8000

# Вбити процес (замініть PID на номер з попередньої команди)
taskkill /PID 12345 /F

# Або використайте інший порт
uvicorn app.main:app --port 8001 --reload
```

---

### ❌ Проблема: Модуль не знайдено (ModuleNotFoundError)

**Симптоми:**
```
ModuleNotFoundError: No module named 'fastapi'
```

**Рішення:**
```powershell
# Перевірте чи активоване venv
.\.venv\Scripts\Activate.ps1

# Переінсталюйте залежності
pip install -r requirements.txt

# Перевірте встановлені пакети
pip list
```

---

### ❌ Проблема: JWT_SECRET занадто короткий

**Симптоми:**
```
ValueError: JWT_SECRET must be at least 32 characters long
```

**Рішення:**
```powershell
# Згенеруйте новий секрет (PowerShell)
$bytes = [System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32)
$secret = [System.BitConverter]::ToString($bytes).Replace("-", "").ToLower()
Write-Host "JWT_SECRET=$secret"

# Або через Python
python -c "import secrets; print(secrets.token_hex(32))"

# Додайте в .env файл
```

---

### ❌ Проблема: Міграції не застосовуються

**Симптоми:**
```
sqlalchemy.exc.ProgrammingError: (pymysql.err.ProgrammingError) (1146, "Table 'diploma_db.alembic_version' doesn't exist")
```

**Рішення:**
```powershell
# Перевірте підключення до БД
alembic current

# Застосуйте міграції знову
alembic upgrade head

# Якщо проблема залишається - скиньте міграції
alembic downgrade base
alembic upgrade head
```

---

## 🛠️ Корисні команди

### Управління сервером

```powershell
# Запустити з автоперезавантаженням
uvicorn app.main:app --reload

# Запустити на певному порту
uvicorn app.main:app --port 8001 --reload

# Запустити з логами DEBUG
uvicorn app.main:app --reload --log-level debug

# Запустити без reload (швидше)
uvicorn app.main:app
```

### Робота з базою даних

```powershell
# Створити нову міграцію
alembic revision --autogenerate -m "назва_міграції"

# Застосувати всі міграції
alembic upgrade head

# Відкотити останню міграцію
alembic downgrade -1

# Показати поточну версію
alembic current

# Показати історію міграцій
alembic history
```

### Очищення проекту

```powershell
# Очистити __pycache__ директорії
.\clear_cache.bat

# Або вручну
Get-ChildItem -Path . -Include __pycache__ -Recurse -Force | Remove-Item -Recurse -Force
Get-ChildItem -Path . -Include *.pyc -Recurse -Force | Remove-Item -Force
```

### Тестування API

```powershell
# Через curl (якщо встановлено)
curl http://localhost:8000/health

# Через PowerShell
Invoke-RestMethod -Uri http://localhost:8000/health

# Login
$body = @{username="admin"; password="admin123"} | ConvertTo-Json
Invoke-RestMethod -Uri http://localhost:8000/api/auth/login -Method Post -Body $body -ContentType "application/json"
```

### Перегляд логів

```powershell
# Якщо запущено через uvicorn - логи в консолі
# Для збереження логів у файл:
uvicorn app.main:app --reload > logs/server.log 2>&1

# Перегляд останніх логів
Get-Content logs/server.log -Tail 50 -Wait
```

---

## 📊 Структура проекту

```
backend/
├── app/                      # Основний код застосунку
│   ├── api/                  # API роутери та ендпоінти
│   │   ├── routes/          # Окремі роути (auth, employees, etc.)
│   │   └── deps.py          # Залежності (JWT перевірка)
│   ├── core/                # Ядро застосунку
│   │   ├── config.py        # Конфігурація з .env
│   │   ├── security.py      # JWT, хешування паролів
│   │   └── logging.py       # Налаштування логування
│   ├── crud/                # CRUD операції з БД
│   ├── db/                  # Налаштування БД
│   ├── models/              # SQLAlchemy моделі (таблиці)
│   ├── schemas/             # Pydantic схеми (валідація)
│   ├── services/            # Бізнес-логіка
│   └── main.py              # Точка входу FastAPI
├── alembic/                 # Міграції бази даних
│   └── versions/            # Файли міграцій
├── docs/                    # Документація
├── .env                     # Змінні оточення (НЕ в git!)
├── .env.example             # Приклад .env файлу
├── requirements.txt         # Python залежності
├── alembic.ini             # Конфігурація Alembic
└── run_dev.bat             # Скрипт запуску (Windows)
```

---

## 🎯 Наступні кроки

Після успішного запуску:

1. ✅ **Протестуйте API** - через Swagger UI (http://localhost:8000/docs)
2. ✅ **Створіть співробітників** - через endpoint `/api/employees`
3. ✅ **Додайте терміналів** - через endpoint `/api/terminals`
4. ✅ **Налаштуйте графіки** - через endpoint `/api/schedules`
5. ✅ **Перегляньте документацію** - файли в папці `docs/`

### Для production:
📖 Перегляньте [02_DEPLOYMENT_НА_СЕРВЕР.md](./02_DEPLOYMENT_НА_СЕРВЕР.md)

### Для роботи з БД:
📖 Перегляньте [03_УПРАВЛІННЯ_БАЗОЮ_ДАНИХ.md](./03_УПРАВЛІННЯ_БАЗОЮ_ДАНИХ.md)

---

## 💡 Підказки

### 🔍 Де шукати помилки?

1. **Консоль сервера** - всі помилки виводяться тут
2. **Swagger UI** - показує помилки API запитів
3. **MySQL логи** - для проблем з БД
4. **Файл .env** - перевірте правильність налаштувань

### 🚀 Поліпшення продуктивності

```ini
# .env - для локальної розробки
DEBUG=true           # Детальні помилки
SQL_ECHO=false       # Не виводити SQL запити (швидше)
LOG_LEVEL=INFO       # Мінімум логів
```

### 🔐 Безпека (development)

```ini
# .env - для локальної розробки
ADMIN_PASSWORD=admin123           # Простий пароль - ОК
JWT_SECRET=будь-що-довше-32-символів  # Не критично
ALLOWED_ORIGINS=*                 # Дозволити всі - ОК
```

---

## 📞 Допомога

### Якщо щось не працює:

1. 📖 Перегляньте цю документацію ще раз
2. 🔍 Перевірте логи сервера
3. 🗄️ Перевірте підключення до БД
4. 🧹 Очистіть кеш: `.\clear_cache.bat`
5. 🔄 Перезапустіть сервер

### Корисні файли документації:

- `01_ШВИДКИЙ_СТАРТ.md` - ця сторінка
- `02_DEPLOYMENT_НА_СЕРВЕР.md` - деплой на production
- `03_УПРАВЛІННЯ_БАЗОЮ_ДАНИХ.md` - робота з MySQL
- `04_ВИПРАВЛЕННЯ_ПРОБЛЕМ.md` - детальний troubleshooting

---

**Успіхів з запуском! 🎉**

Створено: 30 січня 2026  
Версія документації: 2.0
