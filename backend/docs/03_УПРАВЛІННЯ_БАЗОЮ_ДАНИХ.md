# 🗄️ УПРАВЛІННЯ БАЗОЮ ДАНИХ

**Дата оновлення:** 30 січня 2026  
**Версія:** 2.0

---

## 📋 ЗМІСТ

1. [Backup (Резервне копіювання)](#-backup-резервне-копіювання)
2. [Restore (Відновлення)](#-restore-відновлення)
3. [Очищення даних](#-очищення-даних)
4. [Корисні SQL запити](#-корисні-sql-запити)
5. [Оптимізація та обслуговування](#-оптимізація-та-обслуговування)
6. [Моніторинг](#-моніторинг)
7. [Автоматизація](#-автоматизація)

---

## 💾 BACKUP (Резервне копіювання)

### 1. Повний backup через mysqldump

#### Windows (PowerShell):

```powershell
# Змінні
$dbName = "diploma_db"
$dbUser = "timetracker"
$dbPass = "StrongPass123!"
$timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
$backupDir = "F:\Вуз\ДИПЛОМ\SYSTEM\backend\backups"
$backupFile = "$backupDir\backup_$timestamp.sql"

# Створити директорію якщо не існує
if (!(Test-Path $backupDir)) {
    New-Item -ItemType Directory -Path $backupDir
}

# Створити backup
mysqldump -u $dbUser -p$dbPass $dbName > $backupFile

Write-Host "✓ Backup створено: $backupFile" -ForegroundColor Green

# Стиснути backup (опціонально)
Compress-Archive -Path $backupFile -DestinationPath "$backupFile.zip"
Remove-Item $backupFile
Write-Host "✓ Backup стиснуто: $backupFile.zip" -ForegroundColor Green
```

#### Linux:

```bash
# Змінні
DB_NAME="timetracker_prod"
DB_USER="timetracker"
DB_PASS="StrongPass123!"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="$HOME/backups"
BACKUP_FILE="$BACKUP_DIR/backup_$TIMESTAMP.sql"

# Створити директорію
mkdir -p $BACKUP_DIR

# Створити backup
mysqldump -u $DB_USER -p$DB_PASS $DB_NAME > $BACKUP_FILE

echo "✓ Backup створено: $BACKUP_FILE"

# Стиснути
gzip $BACKUP_FILE
echo "✓ Backup стиснуто: $BACKUP_FILE.gz"
```

### 2. Backup через MySQL Workbench

1. Відкрити MySQL Workbench
2. Підключитись до бази даних
3. **Server** → **Data Export**
4. Вибрати базу `diploma_db`
5. **Export to Self-Contained File**
6. Вказати шлях: `F:\Вуз\ДИПЛОМ\SYSTEM\backend\backups\backup_manual.sql`
7. **Start Export**

### 3. Backup окремих таблиць

```powershell
# Windows - backup тільки таблиці events
mysqldump -u timetracker -pStrongPass123! diploma_db events > backups\events_backup.sql

# Або кілька таблиць
mysqldump -u timetracker -pStrongPass123! diploma_db events employees terminals > backups\partial_backup.sql
```

```bash
# Linux
mysqldump -u timetracker -pStrongPass123! timetracker_prod events > backups/events_backup.sql
```

### 4. Backup з автоматичною ротацією

**PowerShell скрипт (auto_backup.ps1):**

```powershell
# Параметри
$dbName = "diploma_db"
$dbUser = "timetracker"
$dbPass = "StrongPass123!"
$backupDir = "F:\Вуз\ДИПЛОМ\SYSTEM\backend\backups"
$maxBackups = 10  # Зберігати тільки останні 10

# Створити backup
$timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
$backupFile = "$backupDir\backup_$timestamp.sql"

New-Item -ItemType Directory -Path $backupDir -Force | Out-Null

Write-Host "Створення backup..." -ForegroundColor Yellow
mysqldump -u $dbUser -p$dbPass $dbName > $backupFile

if ($LASTEXITCODE -eq 0) {
    Write-Host "✓ Backup створено: $backupFile" -ForegroundColor Green
    
    # Стиснути
    Compress-Archive -Path $backupFile -DestinationPath "$backupFile.zip" -Force
    Remove-Item $backupFile
    
    # Видалити старі backups
    $backups = Get-ChildItem "$backupDir\backup_*.sql.zip" | 
        Sort-Object LastWriteTime -Descending
    
    if ($backups.Count -gt $maxBackups) {
        $backups | Select-Object -Skip $maxBackups | Remove-Item
        Write-Host "✓ Старі backups очищено" -ForegroundColor Green
    }
} else {
    Write-Host "✗ Backup не вдався!" -ForegroundColor Red
}
```

**Запуск:**

```powershell
.\auto_backup.ps1
```

---

## 🔄 RESTORE (Відновлення)

### 1. Повне відновлення з backup

#### Windows:

```powershell
# Знайти останній backup
$latestBackup = Get-ChildItem "F:\Вуз\ДИПЛОМ\SYSTEM\backend\backups\*.sql" | 
    Sort-Object LastWriteTime -Descending | 
    Select-Object -First 1

if ($latestBackup) {
    Write-Host "Відновлення з: $($latestBackup.Name)" -ForegroundColor Yellow
    
    # Розпакувати якщо zip
    if ($latestBackup.Extension -eq ".zip") {
        Expand-Archive -Path $latestBackup.FullName -DestinationPath "temp" -Force
        $sqlFile = Get-ChildItem "temp\*.sql" | Select-Object -First 1
    } else {
        $sqlFile = $latestBackup
    }
    
    # Відновити
    mysql -u timetracker -pStrongPass123! diploma_db < $sqlFile.FullName
    
    Write-Host "✓ База даних відновлена!" -ForegroundColor Green
    
    # Очистити temp
    if (Test-Path "temp") { Remove-Item -Recurse "temp" }
} else {
    Write-Host "✗ Backup не знайдено!" -ForegroundColor Red
}
```

#### Linux:

```bash
# Знайти останній backup
LATEST_BACKUP=$(ls -t ~/backups/backup_*.sql.gz | head -1)

if [ -f "$LATEST_BACKUP" ]; then
    echo "Відновлення з: $LATEST_BACKUP"
    
    # Розпакувати та відновити
    gunzip < $LATEST_BACKUP | mysql -u timetracker -pStrongPass123! timetracker_prod
    
    echo "✓ База даних відновлена!"
else
    echo "✗ Backup не знайдено!"
fi
```

### 2. Відновлення через MySQL Workbench

1. Відкрити MySQL Workbench
2. Підключитись до бази даних
3. **Server** → **Data Import**
4. Вибрати **Import from Self-Contained File**
5. Вказати файл backup: `backups\backup_20260130_120000.sql`
6. **Start Import**

### 3. Відновлення окремих таблиць

```powershell
# Відновити тільки таблицю events
mysql -u timetracker -pStrongPass123! diploma_db < backups\events_backup.sql
```

---

## 🧹 ОЧИЩЕННЯ ДАНИХ

### ⚠️ ВАЖЛИВО: Завжди робіть backup перед очищенням!

### 1. Видалити всі події

```sql
-- КРОК 1: Створити backup
-- mysqldump -u timetracker -pStrongPass123! diploma_db > backups/before_cleanup.sql

-- КРОК 2: Відключити перевірку foreign keys
SET FOREIGN_KEY_CHECKS = 0;

-- КРОК 3: Очистити таблицю
TRUNCATE TABLE events;

-- КРОК 4: Увімкнути перевірку foreign keys
SET FOREIGN_KEY_CHECKS = 1;

-- КРОК 5: Перевірити
SELECT COUNT(*) as remaining_events FROM events;
-- Має бути 0
```

### 2. Видалити старі події (старіше 30 днів)

```sql
-- Створити backup старих подій
CREATE TABLE events_backup_old AS 
SELECT * FROM events 
WHERE ts < DATE_SUB(NOW(), INTERVAL 30 DAY);

-- Перевірити скільки буде видалено
SELECT COUNT(*) FROM events WHERE ts < DATE_SUB(NOW(), INTERVAL 30 DAY);

-- Видалити
DELETE FROM events WHERE ts < DATE_SUB(NOW(), INTERVAL 30 DAY);

-- Перевірити результат
SELECT 
    (SELECT COUNT(*) FROM events_backup_old) as deleted_count,
    (SELECT COUNT(*) FROM events) as remaining_count;
```

### 3. Видалити події конкретного співробітника

```sql
-- Знайти ID співробітника
SELECT id, full_name FROM employees WHERE full_name LIKE '%Іванов%';
-- Припустимо ID = 5

-- Backup подій цього співробітника
CREATE TABLE events_backup_employee_5 AS 
SELECT * FROM events WHERE employee_id = 5;

-- Видалити
DELETE FROM events WHERE employee_id = 5;

-- Перевірити
SELECT COUNT(*) FROM events WHERE employee_id = 5;
-- Має бути 0
```

### 4. Видалити ручні події

```sql
-- Backup ручних подій
CREATE TABLE events_backup_manual AS 
SELECT * FROM events WHERE is_manual = 1;

-- Видалити
DELETE FROM events WHERE is_manual = 1;

-- Перевірити
SELECT COUNT(*) FROM events WHERE is_manual = 1;
```

### 5. Очистити ВСІ таблиці (RESET БД)

```sql
-- ⚠️ КРИТИЧНА ОПЕРАЦІЯ! ВИДАЛИТЬ ВСІ ДАНІ!

-- ОБОВ'ЯЗКОВО зробіть backup:
-- mysqldump -u timetracker -pStrongPass123! diploma_db > backups/full_backup_before_reset.sql

SET FOREIGN_KEY_CHECKS = 0;

-- Очистити всі таблиці
TRUNCATE TABLE events;
TRUNCATE TABLE schedules;
TRUNCATE TABLE employees;
TRUNCATE TABLE terminals;
-- НЕ чистимо users (залишити адмін доступ)

SET FOREIGN_KEY_CHECKS = 1;

-- Перевірити
SELECT 'events' as table_name, COUNT(*) as count FROM events
UNION ALL SELECT 'schedules', COUNT(*) FROM schedules
UNION ALL SELECT 'employees', COUNT(*) FROM employees
UNION ALL SELECT 'terminals', COUNT(*) FROM terminals
UNION ALL SELECT 'users', COUNT(*) FROM users;
```

---

## 📊 КОРИСНІ SQL ЗАПИТИ

### 1. Статистика по таблицях

```sql
-- Кількість записів у кожній таблиці
SELECT 
    'users' as table_name, 
    COUNT(*) as records,
    MAX(id) as max_id
FROM users
UNION ALL
SELECT 'employees', COUNT(*), MAX(id) FROM employees
UNION ALL
SELECT 'terminals', COUNT(*), MAX(id) FROM terminals
UNION ALL
SELECT 'events', COUNT(*), MAX(id) FROM events
UNION ALL
SELECT 'schedules', COUNT(*), MAX(id) FROM schedules;
```

### 2. Розмір таблиць

```sql
-- Розмір кожної таблиці в MB
SELECT 
    table_name AS 'Таблиця',
    ROUND(((data_length + index_length) / 1024 / 1024), 2) AS 'Розмір (MB)',
    table_rows AS 'Рядків'
FROM information_schema.TABLES
WHERE table_schema = 'diploma_db'
ORDER BY (data_length + index_length) DESC;
```

### 3. Останні події

```sql
-- Останні 20 подій з деталями
SELECT 
    e.id,
    emp.full_name as 'Співробітник',
    t.name as 'Термінал',
    e.direction as 'Напрямок',
    e.ts as 'Час події',
    CASE 
        WHEN e.is_manual = 1 THEN 'Ручна'
        ELSE 'Автоматична'
    END as 'Тип'
FROM events e
LEFT JOIN employees emp ON e.employee_id = emp.id
LEFT JOIN terminals t ON e.terminal_id = t.id
ORDER BY e.ts DESC
LIMIT 20;
```

### 4. Події за сьогодні

```sql
-- Всі події за сьогодні, згруповані по співробітниках
SELECT 
    emp.full_name as 'Співробітник',
    COUNT(*) as 'Кількість подій',
    MIN(CASE WHEN e.direction = 'IN' THEN e.ts END) as 'Перший прихід',
    MAX(CASE WHEN e.direction = 'OUT' THEN e.ts END) as 'Останній вихід'
FROM events e
JOIN employees emp ON e.employee_id = emp.id
WHERE DATE(e.ts) = CURDATE()
GROUP BY emp.id, emp.full_name
ORDER BY emp.full_name;
```

### 5. Співробітники з кількістю подій

```sql
-- Топ-20 співробітників по кількості подій
SELECT 
    emp.id,
    emp.full_name as 'Співробітник',
    emp.position as 'Посада',
    COUNT(e.id) as 'Всього подій',
    COUNT(CASE WHEN e.direction = 'IN' THEN 1 END) as 'Входів',
    COUNT(CASE WHEN e.direction = 'OUT' THEN 1 END) as 'Виходів',
    MAX(e.ts) as 'Остання подія'
FROM employees emp
LEFT JOIN events e ON emp.id = e.employee_id
GROUP BY emp.id, emp.full_name, emp.position
ORDER BY COUNT(e.id) DESC
LIMIT 20;
```

### 6. Активність термінів

```sql
-- Статистика по терміналах
SELECT 
    t.id,
    t.name as 'Термінал',
    t.location as 'Локація',
    t.is_active as 'Активний',
    COUNT(e.id) as 'Всього подій',
    MAX(e.ts) as 'Остання подія',
    DATEDIFF(NOW(), MAX(e.ts)) as 'Днів без активності'
FROM terminals t
LEFT JOIN events e ON t.id = e.terminal_id
GROUP BY t.id, t.name, t.location, t.is_active
ORDER BY COUNT(e.id) DESC;
```

### 7. Пошук дублікатів подій

```sql
-- Знайти події одного співробітника з однаковим напрямком за короткий час
SELECT 
    e1.id as 'ID 1',
    e2.id as 'ID 2',
    emp.full_name as 'Співробітник',
    e1.direction as 'Напрямок',
    e1.ts as 'Час 1',
    e2.ts as 'Час 2',
    TIMESTAMPDIFF(SECOND, e1.ts, e2.ts) as 'Різниця (сек)'
FROM events e1
JOIN events e2 ON 
    e1.employee_id = e2.employee_id 
    AND e1.direction = e2.direction
    AND e1.id < e2.id
    AND TIMESTAMPDIFF(SECOND, e1.ts, e2.ts) < 60
JOIN employees emp ON e1.employee_id = emp.id
ORDER BY e1.ts DESC
LIMIT 50;
```

### 8. Робочі години за період

```sql
-- Робочі години кожного співробітника за останній тиждень
SELECT 
    emp.full_name as 'Співробітник',
    DATE(e_in.ts) as 'Дата',
    MIN(e_in.ts) as 'Перший вхід',
    MAX(e_out.ts) as 'Останній вихід',
    ROUND(TIMESTAMPDIFF(MINUTE, MIN(e_in.ts), MAX(e_out.ts)) / 60.0, 2) as 'Годин'
FROM employees emp
LEFT JOIN events e_in ON emp.id = e_in.employee_id AND e_in.direction = 'IN'
LEFT JOIN events e_out ON emp.id = e_out.employee_id 
    AND e_out.direction = 'OUT'
    AND DATE(e_out.ts) = DATE(e_in.ts)
WHERE e_in.ts >= DATE_SUB(CURDATE(), INTERVAL 7 DAY)
GROUP BY emp.id, emp.full_name, DATE(e_in.ts)
ORDER BY DATE(e_in.ts) DESC, emp.full_name;
```

---

## ⚙️ ОПТИМІЗАЦІЯ ТА ОБСЛУГОВУВАННЯ

### 1. Оптимізація таблиць

```sql
-- Оптимізувати всі таблиці
OPTIMIZE TABLE users;
OPTIMIZE TABLE employees;
OPTIMIZE TABLE terminals;
OPTIMIZE TABLE events;
OPTIMIZE TABLE schedules;

-- Або всі разом
OPTIMIZE TABLE users, employees, terminals, events, schedules;

-- Результат:
-- Table               Op        Msg_type  Msg_text
-- diploma_db.users    optimize  status    OK
-- diploma_db.events   optimize  status    OK
```

### 2. Аналіз таблиць

```sql
-- Оновити статистику для оптимізатора запитів
ANALYZE TABLE events;
ANALYZE TABLE employees;
ANALYZE TABLE schedules;

-- Результат показує чи потрібна оптимізація
```

### 3. Перевірка та відновлення таблиць

```sql
-- Перевірити таблиці на помилки
CHECK TABLE events;
CHECK TABLE employees;

-- Якщо є помилки - відновити
REPAIR TABLE events;
REPAIR TABLE employees;
```

### 4. Перебудова індексів

```sql
-- Подивитись індекси
SHOW INDEX FROM events;
SHOW INDEX FROM employees;

-- Перебудувати таблицю (пересоздає індекси)
ALTER TABLE events ENGINE=InnoDB;
```

### 5. Очищення MySQL логів

```sql
-- Показати бінарні логи
SHOW BINARY LOGS;

-- Видалити логи старіше 7 днів
PURGE BINARY LOGS BEFORE DATE(NOW() - INTERVAL 7 DAY);

-- Або видалити всі крім останніх 3
PURGE BINARY LOGS TO 'mysql-bin.000003';
```

---

## 📈 МОНІТОРИНГ

### 1. Моніторинг розміру бази даних

```sql
-- Загальний розмір бази
SELECT 
    table_schema AS 'База даних',
    ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS 'Розмір (MB)',
    ROUND(SUM(data_length) / 1024 / 1024, 2) AS 'Дані (MB)',
    ROUND(SUM(index_length) / 1024 / 1024, 2) AS 'Індекси (MB)'
FROM information_schema.TABLES
WHERE table_schema = 'diploma_db'
GROUP BY table_schema;
```

### 2. Моніторинг активних підключень

```sql
-- Показати всі підключення
SHOW PROCESSLIST;

-- Детальніше
SELECT 
    ID as 'ID',
    USER as 'Користувач',
    HOST as 'Хост',
    DB as 'БД',
    COMMAND as 'Команда',
    TIME as 'Час (сек)',
    STATE as 'Стан',
    LEFT(INFO, 50) as 'Запит'
FROM information_schema.PROCESSLIST
WHERE DB = 'diploma_db'
ORDER BY TIME DESC;
```

### 3. Моніторинг повільних запитів

```sql
-- Увімкнути логування повільних запитів
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 2;  -- запити довше 2 секунд

-- Перевірити налаштування
SHOW VARIABLES LIKE 'slow_query%';
SHOW VARIABLES LIKE 'long_query_time';

-- Подивитись лог
-- Linux: tail -f /var/log/mysql/slow.log
-- Windows: відкрити файл із каталогу MySQL
```

### 4. Статус MySQL

```sql
-- Показати статус сервера
SHOW STATUS;

-- Конкретні метрики
SHOW STATUS LIKE 'Threads_connected';    -- Активні підключення
SHOW STATUS LIKE 'Questions';            -- Всього запитів
SHOW STATUS LIKE 'Uptime';               -- Час роботи сервера
SHOW STATUS LIKE 'Slow_queries';         -- Повільні запити
```

---

## 🤖 АВТОМАТИЗАЦІЯ

### 1. Автоматичний щоденний backup (Windows Task Scheduler)

**Крок 1: Створити скрипт backup**

Збережіть як `F:\Вуз\ДИПЛОМ\SYSTEM\backend\auto_backup.ps1`:

```powershell
# Параметри
$dbName = "diploma_db"
$dbUser = "timetracker"
$dbPass = "StrongPass123!"
$backupDir = "F:\Вуз\ДИПЛОМ\SYSTEM\backend\backups"
$maxBackups = 30  # Зберігати 30 днів

# Логування
$logFile = "$backupDir\backup.log"
$timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"

# Створити backup
$backupTimestamp = Get-Date -Format "yyyyMMdd_HHmmss"
$backupFile = "$backupDir\backup_$backupTimestamp.sql"

try {
    # Backup
    Write-Output "$timestamp - Початок backup..." | Out-File -Append $logFile
    mysqldump -u $dbUser -p$dbPass $dbName > $backupFile
    
    # Стиснути
    Compress-Archive -Path $backupFile -DestinationPath "$backupFile.zip" -Force
    Remove-Item $backupFile
    
    Write-Output "$timestamp - ✓ Backup створено: $backupFile.zip" | Out-File -Append $logFile
    
    # Очистка старих
    $backups = Get-ChildItem "$backupDir\backup_*.sql.zip" | 
        Sort-Object LastWriteTime -Descending
    
    if ($backups.Count -gt $maxBackups) {
        $toDelete = $backups | Select-Object -Skip $maxBackups
        $toDelete | Remove-Item
        Write-Output "$timestamp - ✓ Видалено старих backups: $($toDelete.Count)" | Out-File -Append $logFile
    }
    
} catch {
    Write-Output "$timestamp - ✗ Помилка: $_" | Out-File -Append $logFile
}
```

**Крок 2: Створити Task в Task Scheduler**

```powershell
# Виконати PowerShell від адміністратора:

$action = New-ScheduledTaskAction -Execute "PowerShell.exe" `
    -Argument "-ExecutionPolicy Bypass -File F:\Вуз\ДИПЛОМ\SYSTEM\backend\auto_backup.ps1"

$trigger = New-ScheduledTaskTrigger -Daily -At 2AM

$settings = New-ScheduledTaskSettingsSet -StartWhenAvailable

Register-ScheduledTask `
    -TaskName "TimeTracker_DailyBackup" `
    -Action $action `
    -Trigger $trigger `
    -Settings $settings `
    -Description "Щоденний backup бази даних TimeTracker"

Write-Host "✓ Task створено!" -ForegroundColor Green
```

### 2. Автоматичний backup (Linux cron)

```bash
# Відкрити crontab
crontab -e

# Додати рядок (щоденно о 2:00)
0 2 * * * /home/timetracker/backup_db.sh >> /home/timetracker/backup.log 2>&1

# Або щотижня (неділя о 3:00)
0 3 * * 0 /home/timetracker/backup_db.sh >> /home/timetracker/backup.log 2>&1
```

### 3. Автоматична очистка старих подій (> 90 днів)

**SQL Event (виконується автоматично):**

```sql
-- Створити event
DELIMITER $$

CREATE EVENT IF NOT EXISTS cleanup_old_events
ON SCHEDULE EVERY 1 DAY
STARTS CURRENT_DATE + INTERVAL 1 DAY + INTERVAL 3 HOUR
DO
BEGIN
    -- Видалити події старіше 90 днів
    DELETE FROM events 
    WHERE ts < DATE_SUB(NOW(), INTERVAL 90 DAY)
    LIMIT 1000;  -- Обмеження щоб не перевантажити БД
END$$

DELIMITER ;

-- Перевірити чи працює
SHOW EVENTS;

-- Увімкнути event scheduler (якщо вимкнено)
SET GLOBAL event_scheduler = ON;
```

---

## 📋 ЧЕКЛИСТ ОБСЛУГОВУВАННЯ БД

### Щоденно:
- [ ] Автоматичний backup (через Task Scheduler або cron)
- [ ] Перевірка розміру бази даних
- [ ] Перевірка логів на помилки

### Щотижня:
- [ ] Перевірка статусу MySQL: `SHOW STATUS;`
- [ ] Аналіз таблиць: `ANALYZE TABLE events, employees;`
- [ ] Перевірка повільних запитів

### Щомісяця:
- [ ] Оптимізація таблиць: `OPTIMIZE TABLE events;`
- [ ] Перевірка таблиць: `CHECK TABLE events;`
- [ ] Очищення старих backup (>30 днів)
- [ ] Перевірка розміру бінарних логів
- [ ] Тестове відновлення з backup

### Щоквартально:
- [ ] Повний аудит безпеки БД
- [ ] Перевірка індексів та їх ефективності
- [ ] Аналіз планів запитів (EXPLAIN)
- [ ] Оновлення MySQL до останньої версії

---

**Успіхів з управлінням базою даних! 💾**

Створено: 30 січня 2026  
Версія документації: 2.0
