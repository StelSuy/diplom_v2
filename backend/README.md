# TimeTracker Backend API

Система учёта рабочего времени с поддержкой NFC-терминалов и веб-администрирования.

## 🚀 Быстрый старт

### Требования
- Python 3.12+
- MySQL/MariaDB 8.0+
- pip

### Установка

1. **Клонировать репозиторий** (если ещё не сделано)

2. **Установить зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настроить базу данных:**

Создать файл `.env` на основе `.env.example`:
```env
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/timetracker
JWT_SECRET=your-secret-key-min-32-chars
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

4. **Инициализировать БД:**
```bash
python init_db.py
```

5. **Запустить сервер:**
```bash
# Windows
run_dev.bat

# Linux/Mac
./run_dev.sh

# Или напрямую
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен на: `http://localhost:8000`

---

## 📚 Документация API

- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
- **OpenAPI JSON:** http://localhost:8000/openapi.json

---

## 🔐 Аутентификация

### Для админ-панели (JWT)

1. **Получить токен:**
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

2. **Использовать токен:**
```http
GET /api/employees/
Authorization: Bearer <your_token>
```

### Для терминалов (API Key)

Терминалы используют заголовок `X-Terminal-Key`:
```http
POST /api/events/nfc
X-Terminal-Key: <terminal_api_key>
Content-Type: application/json
```

---

## 📋 Основные эндпоинты

### Аутентификация
- `POST /api/auth/login` - Вход администратора

### Сотрудники
- `GET /api/employees/` - Список сотрудников
- `POST /api/employees/` - Создать сотрудника
- `GET /api/employees/{id}` - Получить сотрудника
- `PATCH /api/employees/{id}` - Обновить сотрудника

### События (вход/выход)
- `POST /api/events/nfc` - Регистрация события от терминала
- `POST /api/events/secure` - Безопасная регистрация (с challenge-response)

### Расписание
- `GET /api/schedule` - Получить расписание
- `POST /api/schedule` - **Массовое сохранение ячеек** ⭐ NEW
- `POST /api/schedule/cell` - Сохранить одну ячейку
- `DELETE /api/schedule/cell` - Удалить ячейку
- `GET /api/schedule/pdf` - Экспорт в PDF

### Статистика
- `GET /api/stats/employee/{id}` - Общая статистика сотрудника
- `GET /api/stats/employee/{id}/daily` - Дневная статистика

### Терминалы
- `GET /api/terminals/` - Список терминалов
- `POST /api/terminals/` - Создать терминал
- `DELETE /api/terminals/{id}` - Удалить терминал

---

## 🆕 Массовое сохранение расписания

### Формат запроса:
```http
POST /api/schedule
Authorization: Bearer <token>
Content-Type: application/json

{
  "cells": [
    {
      "employee_id": 1,
      "day": "2026-01-02",
      "code": "8-17"
    },
    {
      "employee_id": 2,
      "day": "2026-01-02",
      "start_hhmm": "09:00",
      "end_hhmm": "18:00",
      "code": "ОФ"
    }
  ]
}
```

### Формат ответа:
```json
{
  "success": 2,
  "failed": 0,
  "errors": []
}
```

### Поддерживаемые форматы:

1. **Код в формате H-H:**
   - `"8-17"` → автоконверт в `08:00 - 17:00`
   - `"9-18"` → автоконверт в `09:00 - 18:00`

2. **Явное указание времени:**
   ```json
   {
     "start_hhmm": "09:00",
     "end_hhmm": "18:00",
     "code": "ОФ"
   }
   ```

3. **Удаление ячейки:**
   ```json
   {
     "employee_id": 1,
     "day": "2026-01-02",
     "code": "",
     "start_hhmm": "",
     "end_hhmm": ""
   }
   ```

---

## 🏗️ Структура проекта

```
backend/
├── app/
│   ├── api/           # API роуты
│   │   ├── deps.py    # Зависимости (авторизация)
│   │   ├── router.py  # Главный роутер
│   │   └── routes/    # Модули роутов
│   ├── core/          # Ядро приложения
│   │   ├── config.py  # Конфигурация
│   │   ├── security.py # JWT, хеширование
│   │   ├── time.py    # Работа с timezone
│   │   └── seed.py    # Начальные данные
│   ├── crud/          # CRUD операции
│   ├── db/            # База данных
│   │   ├── base.py    # Base model
│   │   ├── session.py # Сессии
│   │   └── init_db.py # Инициализация
│   ├── models/        # SQLAlchemy модели
│   ├── schemas/       # Pydantic схемы
│   ├── services/      # Бизнес-логика
│   ├── security/      # Криптография
│   ├── static/        # Статика (админка)
│   └── main.py        # Точка входа
├── requirements.txt   # Зависимости
├── init_db.py        # Скрипт инициализации БД
├── .env              # Переменные окружения
└── README.md         # Этот файл
```

---

## 🧪 Тестирование

### Ручное тестирование (curl)
См. файл `TESTING_CHECKLIST.md`

### Автоматические тесты
```bash
pytest tests/ -v
```

---

## 🔧 Разработка

### Запуск в режиме разработки
```bash
uvicorn app.main:app --reload --log-level debug
```

### Миграции БД (Alembic)
```bash
# Создать миграцию
alembic revision --autogenerate -m "description"

# Применить миграции
alembic upgrade head

# Откатить миграцию
alembic downgrade -1
```

### Проверка кода
```bash
# Форматирование
black app/

# Линтинг
pylint app/

# Type checking
mypy app/
```

---

## 📊 Особенности реализации

### 1. Timezone Handling
- Все события хранятся в **UTC**
- Отображение в **Europe/Warsaw**
- Интервалы могут пересекать полночь

### 2. Автоматическое определение направления
Сервер сам определяет IN/OUT на основе последнего события:
- Если последнее событие IN → новое OUT
- Иначе → новое IN

### 3. Детектирование аномалий
Система автоматически детектирует:
- Дублирующийся IN (без закрытия предыдущего)
- OUT без предшествующего IN
- OUT раньше чем IN
- Неизвестное направление

### 4. Автозакрытие смен
- Открытые смены автоматически закрываются в конце дня
- Учитывается при подсчёте статистики

### 5. Challenge-Response аутентификация
- Терминалы могут использовать RSA ключи
- Сервер проверяет подпись challenge
- Защита от replay атак

---

## 🐛 Решение проблем

### База данных не подключается
1. Проверьте `DATABASE_URL` в `.env`
2. Убедитесь что MySQL/MariaDB запущена
3. Проверьте права пользователя БД

### ModuleNotFoundError
```bash
pip install -r requirements.txt
```

### 401 Unauthorized
1. Получите новый токен через `/api/auth/login`
2. Проверьте что токен в заголовке `Authorization: Bearer <token>`

### 405 Method Not Allowed
- Проверьте HTTP метод (GET/POST/PUT/DELETE)
- Убедитесь что эндпоинт существует (см. `/docs`)

---

## 📝 Changelog

### v1.1.0 (2026-01-24)
- ✅ Добавлен `POST /api/schedule` для batch операций
- ✅ Улучшена обработка ошибок в расписании
- ✅ Добавлена валидация формата кода времени

### v1.0.0 (2026-01-01)
- Initial release

---

## 📄 Лицензия

Дипломный проект

---

## 👥 Автор

Ваше имя - Дипломная работа 2026

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Смотрите документацию в `/docs`
3. Изучите файлы в корне проекта:
   - `IMPROVEMENTS.md` - Рекомендации по улучшению
   - `FIX_405_DONE.md` - Исправление ошибки 405
   - `TESTING_CHECKLIST.md` - Чеклист тестирования
   - `QUICK_START.md` - Быстрый старт
   - `SUMMARY.md` - Резюме проекта
